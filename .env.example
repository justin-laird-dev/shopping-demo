# Copy this file to .env and adjust if needed.
# Required for Prisma CLI commands (migrate, db seed, etc.) - the running
# app itself falls back to this same value if DATABASE_URL is unset.
DATABASE_URL="file:./dev.db"
